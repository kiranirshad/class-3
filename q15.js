let weather = hot;
speed = 14;

if(weather! snowing weather ! raining && weather ! cloudy && speed<15){
    console.log("The weather conditions are suitable for rocket");
} else {
    console.log("NOT");
}